<?xml version="1.0" encoding="UTF-8"?>
<!-- Add these dependencies to your pom.xml for MongoDB testing -->
<dependencies>
    <!-- Embedded MongoDB for testing -->
    <dependency>
        <groupId>de.flapdoodle.embed</groupId>
        <artifactId>de.flapdoodle.embed.mongo</artifactId>
        <version>3.5.4</version>
        <scope>test</scope>
    </dependency>

    <!-- MongoDB Driver (if not already present) -->
    <dependency>
        <groupId>org.mongodb</groupId>
        <artifactId>mongodb-driver-sync</artifactId>
        <version>4.9.1</version>
        <scope>test</scope>
    </dependency>

    <!-- Mockito inline for static mocking (optional, for advanced tests) -->
    <dependency>
        <groupId>org.mockito</groupId>
        <artifactId>mockito-inline</artifactId>
        <version>4.11.0</version>
        <scope>test</scope>
    </dependency>

    <!-- PowerMock for static mocking (alternative to mockito-inline) -->
    <dependency>
        <groupId>org.powermock</groupId>
        <artifactId>powermock-module-junit4</artifactId>
        <version>2.0.9</version>
        <scope>test</scope>
    </dependency>
    <dependency>
        <groupId>org.powermock</groupId>
        <artifactId>powermock-api-mockito2</artifactId>
        <version>2.0.9</version>
        <scope>test</scope>
    </dependency>
</dependencies>